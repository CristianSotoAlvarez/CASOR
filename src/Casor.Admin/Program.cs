// Punto de entrada del Admin (gerente).
// Fase 0: generar la app real con las plantillas oficiales de Avalonia:
//   dotnet new install Avalonia.Templates
//   dotnet new avalonia.mvvm --name Casor.Admin --output . --force
// (se dejó el .csproj listo con las dependencias; este archivo se reemplaza)
Console.WriteLine("del Admin (gerente) — pendiente de generar con plantilla Avalonia MVVM (ver README)");
